import React, { useState } from 'react';
import axios from 'axios';
import { connect } from 'react-redux';

const DropDown = props => {

    // import react drop down library for drop down menu

    // integrate redux with dropDown js file to get characters from state

    // onSelection of character in dropDown menu, 
    //  send API request to get description of character and store in state
    // 

    // store that selected character in redux state

    const mapStateToProps = state => {
        return {
            
        }
    }
    
    const mapDispatchToProps = {
        
    }
}

export default connect(mapStateToProps, mapDispatchToProps)(DropDown);